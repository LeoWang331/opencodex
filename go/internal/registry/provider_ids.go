package registry

// Historical provider IDs remain package-level routing vocabulary even though
// tier migration is owned by internal/config.
const (
	OpenAICodexProviderID       = "openai"
	LegacyOpenAIMultiProviderID = "openai-multi"
)
